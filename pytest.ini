[pytest]
markers =
    live: marks tests as live integration tests that require real Azure services
addopts = -m "not live"
filterwarnings =
    ignore::DeprecationWarning
    ignore:When conversation_id is set.*:Warning
    error::UserWarning:agent_framework
log_level = ERROR